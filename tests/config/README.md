# tests.config module

## Purpose
Verifies config parsing, defaults, and environment-secret handling.

## Key files
- `test_app_config_loading.py`: characterization tests for `src/app_config*` modules.

## Configuration
No module-specific configuration.
